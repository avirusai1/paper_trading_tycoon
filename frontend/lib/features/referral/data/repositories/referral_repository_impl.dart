/// Paper Trading Tycoon — Referral Repository Implementation
///
/// Implements the domain-layer [ReferralRepository] interface.
/// Orchestrates between remote and local data sources.
/// Catches [AppException] and maps to [Failure] via [ErrorMapper].
library;

/// Concrete implementation of the referral repository.
final class ReferralRepositoryImpl {
  // Implementation defined per milestone.
}
